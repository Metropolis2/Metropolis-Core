[package]
name = "choice"
version = "0.1.0"
edition = "2021"

[dependencies]
ttf = { path = "../ttf" }
anyhow = "1.0"
log = "0.4"
serde = { version = "1.0", optional = true }
serde_derive = { version = "1.0", optional = true }

[features]
serde-1 = ["serde", "serde_derive"]
